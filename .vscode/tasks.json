{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-build",
			"label": "docker-build",
			"platform": "python",
			"dockerBuild": {
				"tag": "gazpar2mqtt:latest",
				"dockerfile": "${workspaceFolder}/Dockerfile",
				"context": "${workspaceFolder}",
				"pull": true
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: debug",
			"dependsOn": [
				"docker-build"
			],
			"dockerRun": {
				"volumes": [
				  {
					"containerPath": "/data", "localPath": "${workspaceFolder}"
				  }
				]
			},
			"python": {
				"file": "app/gazpar2mqtt.py",
				"args": ["--grdf_username", "comtchr@gmail.com","--grdf_password","SoGaMax60600","--debug", "true","--mqtt_host","192.168.0.201"]
			}
		}
	]
}